import React from 'react';
import { DisclosureManager } from '../features/disclosures/DisclosureManager';

export const Disclosures: React.FC = () => {
  return (
    <div className="p-6">
      <DisclosureManager />
    </div>
  );
};
